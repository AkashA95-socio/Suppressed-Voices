# Copy this file to .env and fill in whichever provider(s) you use.
# run.py / pipeline.py load .env automatically — no need to $env:SET each session.

GEMINI_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
